[package]
name = "node-wait"
version = "0.1.0"
authors = ["themez <xian.zeng@ruguoapp.com>"]
license = "MIT"
build = "build.rs"
exclude = ["artifacts.json", "index.node"]

[lib]
name = "node_wait"
crate-type = ["dylib"]

[build-dependencies]
neon-build = "0.2.0"

[dependencies]
neon = "0.2.0"
